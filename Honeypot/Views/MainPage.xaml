<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Honeypot.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Honeypot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:converters="using:Honeypot.Converters"
    xmlns:models="using:Honeypot.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <Thickness x:Key="NavigationViewContentMargin">0,0,0,0</Thickness>
        <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
        <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>

        <converters:IntToBoolConverter x:Key="IntToBoolConverter"/>

        <models:NavigationItemTemplateSelector x:Key="NavigationItemTemplateSelector">
            <models:NavigationItemTemplateSelector.ItemTemplate>
                <DataTemplate x:DataType="models:NavigationItem">
                    <NavigationViewItem Tag="{x:Bind Tag}" MenuItemsSource="{x:Bind Children,Mode=OneWay}" IsExpanded="{x:Bind Children.Count, Mode=OneWay, Converter={StaticResource IntToBoolConverter}}" ToolTipService.ToolTip="{x:Bind Name}">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="{x:Bind Icon}" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                        </NavigationViewItem.Icon>
                        <NavigationViewItem.Content>
                            <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </NavigationViewItem.Content>
                    </NavigationViewItem>
                </DataTemplate>
            </models:NavigationItemTemplateSelector.ItemTemplate>
            <models:NavigationItemTemplateSelector.HeaderTemplate>
                <DataTemplate x:DataType="models:NavigationHeader">
                    <NavigationViewItemHeader Content="{x:Bind Name}"/>
                </DataTemplate>
            </models:NavigationItemTemplateSelector.HeaderTemplate>
            <models:NavigationItemTemplateSelector.SeparatorTemplate>
                <DataTemplate x:DataType="models:NavigationSeparator">
                    <NavigationViewItemSeparator/>
                </DataTemplate>
            </models:NavigationItemTemplateSelector.SeparatorTemplate>
            <models:NavigationItemTemplateSelector.SettingsItemTemplate>
                <DataTemplate x:DataType="models:NavigationSettingsItem">
                    <NavigationViewItem Tag="{x:Bind Tag}" ToolTipService.ToolTip="{x:Bind Name}">
                        <NavigationViewItem.Icon>
                            <AnimatedIcon>
                                <AnimatedIcon.Source>
                                    <animatedvisuals:AnimatedSettingsVisualSource/>
                                </AnimatedIcon.Source>
                                <AnimatedIcon.FallbackIconSource>
                                    <FontIconSource Glyph="&#xE713;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </AnimatedIcon.FallbackIconSource>
                            </AnimatedIcon>
                        </NavigationViewItem.Icon>
                        <NavigationViewItem.Content>
                            <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </NavigationViewItem.Content>
                    </NavigationViewItem>
                </DataTemplate>
            </models:NavigationItemTemplateSelector.SettingsItemTemplate>
        </models:NavigationItemTemplateSelector>
    </Page.Resources>

    <Grid Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <NavigationView x:Name="MainNavigationView" Background="Transparent" PaneDisplayMode="Left" OpenPaneLength="220" 
                        IsBackButtonVisible="Collapsed" IsPaneToggleButtonVisible="False" IsSettingsVisible="False" IsTitleBarAutoPaddingEnabled="False"
                        MenuItemsSource="{x:Bind ViewModel.Navigation.Items, Mode=OneWay}" 
                        FooterMenuItemsSource="{x:Bind ViewModel.Navigation.FooterItems, Mode=OneWay}"
                        MenuItemTemplateSelector="{StaticResource NavigationItemTemplateSelector}" 
                        Loaded="MainNavigationView_Loaded" ItemInvoked="MainNavigationView_ItemInvoked">
            <NavigationView.PaneCustomContent>
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <StackPanel x:Name="ExpandedLogo" Orientation="Horizontal" Spacing="14" Margin="12,18,12,20" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <Image Height="34" Width="34" VerticalAlignment="Center">
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/HoneypotIcon.png" DecodePixelType="Logical" DecodePixelHeight="68"/>
                            </Image.Source>
                        </Image>
                        <TextBlock x:Uid="AppNameTextBlock" FontSize="17" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel x:Name="CompactLogo" Orientation="Horizontal" Spacing="14" Margin="12,26,12,20" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <Image Height="24" Width="24" VerticalAlignment="Center">
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/HoneypotIcon.png" DecodePixelType="Logical" DecodePixelHeight="48"/>
                            </Image.Source>
                        </Image>
                    </StackPanel>
                </Grid>
            </NavigationView.PaneCustomContent>

            <Frame x:Name="MainFrame" Padding="0,0,0,0" Margin="0,0,0,0">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="944"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainNavigationView.PaneDisplayMode" Value="Left"/>
                        <Setter Target="ExpandedLogo.Visibility" Value="Visible"/>
                        <Setter Target="CompactLogo.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainNavigationView.PaneDisplayMode" Value="LeftCompact"/>
                        <Setter Target="ExpandedLogo.Visibility" Value="Collapsed"/>
                        <Setter Target="CompactLogo.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
