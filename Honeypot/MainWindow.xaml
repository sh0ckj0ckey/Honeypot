<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Honeypot.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Honeypot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Honeypot"
    Closed="Window_Closed">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Window.Content>
        <Grid Loaded="Grid_Loaded" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Frame x:Name="MainFrame" Margin="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Grid x:Name="AppTitleBar" Height="56" Background="Transparent" BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="0,0,0,1" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
        </Grid>
    </Window.Content>
</Window>
